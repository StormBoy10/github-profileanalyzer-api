# Server Configuration
PORT=5000

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=github_analyzer

# GitHub API Configuration (Optional)
# Generating a personal access token (classic or fine-grained) and pasting it here 
# will increase your rate limit to 5000 requests/hour instead of 60/hour.
GITHUB_TOKEN=
